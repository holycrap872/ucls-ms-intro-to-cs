0. The name of the Scratch function is _ and it takes _ inputs
1. The definition of a function is:
    a. Something that takes input and produces output
    b. Something that produces input and output simultaneously
    c. Something that links the name of the function and its inputs
    d. Something that always has an output that is equal to the input
2. How would the math function `g(x)` be represented in Scratch?
3. How would the following Scratch block (function) be represented in math?
    a. f(x, y)
    b. (f, x, y)
    c. f(x)(y)
    d. f(x) --> y
4. A bacon vending machine is like a function because:
    a. It takes money as an input and produces bacon as an output
    b. It requires electricity to run
    c. The bacon is guaranteed to be crispy
    d. You can reverse it by giving it bacon and getting back money
5. Which of the following Scratch blocks would yield the following input/output pairs:
    a. (2, 2) -> 4
    c. (4, 4) -> 8
    b. (6, 1) -> 6
6. Why are functions so powerful?
    a. You don't have to know how they work to use them
    b. They always produce more than a single output
    c. The output is guaranteed to match the input
