0. ​The Scratch block above is a function. The name of the function is _ and it takes _ input(s)
    - Picture of a custom block: `sort(x, y , z)`
0. The definition of a function is:
    - Something that takes input(s) and produces output
    - Something that produces input(s) and output simultaneously
    - Something that links the name of the function and its input(s)
    - Something that always has an output that is equal to the input(s)
0. How would the math function `g(x)` be represented in Scratch?
    - g( ) ( )
    - g
    - g x
    - g ( )
0. How would the Scratch block (function) seen below be represented in math?
    - Picture of custom block: `f ( )`
    - f(x, y)
    - (f, x, y)
    - f(x)(y)
    - f(x) --> y
0. A bacon vending machine is like a function because:
    - It takes money as an input and produces bacon as an output
    - It requires electricity to run
    - The bacon is guaranteed to be crispy
    - You can reverse it by giving it bacon and getting back money
0. Which of the following Scratch blocks (functions) would yield the following input/output pairs:
    - Input: (2, 2) -> Output: 4
    - Input: (4, 4) -> Output: 16
    - Input: (6, 1) -> Output: 6
0. Why are functions so useful (select the **three** that apply)?
    - You don't have to know how they work to use them
    - They help break down complex problems into simpler parts
    - They reduce the amount of memory required to run them
    - They let you reuse code without copying and pasting
    - They allow you to copy your code on the internet
0. If a function takes "speed" and "direction" as inputs, what is it likely trying to do?
    - Control how an object moves
    - Change the object's color
    - Make the object bigger or smaller
    - Create a new object
